<div id='composted'>
<div class="page-header">
  <h1>Share your opinion</h1>
</div>
<%= form_for(@compost, html: {class: "form-horizontal"}) do |f|, remote: true %>
  <% if @compost.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@compost.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
      <% @compost.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
    <%= f.label :body %><br>
    <%= f.text_area :body %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

<%= link_to 'Back', posts_path %>  <!-- posts_path -->
</div>