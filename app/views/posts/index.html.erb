<h1>Listing posts</h1>
<ul class="list-group">
  <li class="list-group-item active"> Author online:<%= link_to  current_user.name.capitalize, user_path(current_user.id) if current_user %>
  <span class="badge">Total posts:<%= @number %></span></li></ul>

 

<% @posts.each do |post| %>

<div class="panel panel-default">
  <div class="panel-body">
  <p><strong><%= post.title.capitalize %></strong></p>
  <p><e><%= post.body %></e></p>
  <p><strong>Tags:</strong><%= post.tags %></p>
<table>
  <tr>
      <td bgcolor="#CCCC99"><%= link_to 'Show', post if current_user %></td>
      <td bgcolor="#CCCCCC"><%= link_to 'Edit', edit_post_path(post) if current_user %></td>
      <td bgcolor="#CCCCFF"><%= link_to 'Destroy', post, method: :delete, data: { confirm: 'Are you sure?' } if current_user %></td> 
      </tr>
    </table>
    Posted by: <%= post.user %>
    <hr>
  </div>
</div>
<% end %>
<h3><%=  link_to 'New Post', new_post_path if current_user %></h3>



 <!-- <table>
  <thead>
    <tr>
      <th><i>Title</i></th>
      <th><i>Body</i></th>
      <th><i>Tags</i></th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
  
    <% @posts.each do |post| %>
      <tr>

        <td bgcolor="#CCCC00"><%= post.title %></td>
        <td bgcolor="#CCCC33"><%= post.body %></td>
        <td bgcolor="#CCCC66"><%= post.tags %></td>
        <td bgcolor="#CCCC99"><%= link_to 'Show', post if current_user %></td>
        <td bgcolor="#CCCCCC"><%= link_to 'Edit', edit_post_path(post) if current_user %></td>
        <td bgcolor="#CCCCFF"><%= link_to 'Destroy', post, method: :delete, data: { confirm: 'Are you sure?' } if current_user %></td> 
      </tr>
    <% end %>
  </tbody>
</table> 

<br>

<h3><%=  link_to 'New Post', new_post_path if current_user %></h3> 

<ul class="list-group">
  <li class="list-group-item active"> Author:<%= link_to  current_user.name.capitalize if current_user %>
  <span class="badge">Total posts:<%= @number %></span></li>
   <% @posts.each do |post| %>

  <li class="list-group-item"><%= post.title %></li>
  <li class="list-group-item"><%= post.body %></li>
  <li class="list-group-item">Tags:<%= post.tags %></li>
</ul>
<% end %>
<h3><%=  link_to 'New Post', new_post_path if current_user %></h3>


 <!-- Author:<%= link_to  current_user.name.capitalize if current_user %>
     <%= link_to "My profile", user_path(current_user.id) if current_user %> -->


  